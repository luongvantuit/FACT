import {StyleSheet} from 'react-native';
const StyledImage = StyleSheet.create({
  rightHeader: {
    width: 40,
    height: 40,
    borderRadius: 8,
  },
  leftHeader: {
    width: 40,
    height: 40,
    borderRadius: 8,
  },
});

export default StyledImage;
