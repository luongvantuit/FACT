import React from 'react'
import { View, Text } from 'react-native'

export default function HeaderDashboard({ navigation, route, layout, options }) {
    return (
        <View>
            <Text></Text>
        </View>
    )
}
